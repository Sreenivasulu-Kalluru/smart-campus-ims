export const metadata = {
  title: "Report an Item",
  description: "Lost something? Found something? Report it here immediately.",
};

export default function ReportLayout({ children }) {
  return <>{children}</>;
}
