export const metadata = {
  title: "My Profile", // Browser tab will show "My Profile | Campus L&F"
  description: "Manage your reported lost and found items.",
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}
